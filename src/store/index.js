import { createStore } from 'vuex'

export default createStore({
  state: {
    visibleSideBar: false,
    permission: {
      dialogForm: false,
    }
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
